<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.QuranApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.QuranApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.QuranApp</item>
    </style>
    
    <style name="Theme.QuranApp.Reading" parent="Theme.AppCompat.DayNight">
        <!-- Reading screen specific theme attributes -->
    </style>
</resources>